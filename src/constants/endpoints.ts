export enum Endpoint {
  user = "/user",
  task = "/task",
  auth = "/auth",
  login = "login",
  completed = "/completed",
  all = "/all",
  change_password = "/change-password",
  deleted = "/deleted",
  notification = "/notification",
}
